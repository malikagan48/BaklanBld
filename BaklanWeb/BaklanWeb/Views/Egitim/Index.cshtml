@model IEnumerable<BaklanWeb.Models.Model.Egitim>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EgitimAciklama)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EgitimAciklama)
        </td>
        <td>
            @Html.ActionLink("Güncelle", "Edit", new { id=item.EgitimId }) 
        </td>
    </tr>
}

</table>
