@model IEnumerable<BaklanWeb.Models.Model.Ihaleler>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IhalelerAciklama)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IhalelerAciklama)
        </td>
        <td>
            @Html.ActionLink("Güncelle", "Edit", new { id=item.IhalelerId }) 
        </td>
    </tr>
}

</table>
