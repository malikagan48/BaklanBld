@model IEnumerable<BaklanWeb.Models.Model.IlanVeReklam>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IlanVeReklamAciklama)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IlanVeReklamAciklama)
        </td>
        <td>
            @Html.ActionLink("Güncelle", "Edit", new { id=item.IlanVeReklamId }) 
        </td>
    </tr>
}

</table>
