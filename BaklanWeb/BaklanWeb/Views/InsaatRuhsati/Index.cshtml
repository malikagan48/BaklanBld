@model IEnumerable<BaklanWeb.Models.Model.InsaatRuhsati>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InsaatRuhsatiAciklama)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InsaatRuhsatiAciklama)
        </td>
        <td>
            @Html.ActionLink("Güncelle", "Edit", new { id=item.InsaatRuhsatiId }) 
        </td>
    </tr>
}

</table>
