@model IEnumerable<BaklanWeb.Models.Model.IsyeriAcmaRuhsati>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IsyeriAcmaRuhsatiAciklama)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IsyeriAcmaRuhsatiAciklama)
        </td>
        <td>
            @Html.ActionLink("Güncelle", "Edit", new { id=item.IsyeriAcmaRuhsatiId })
        </td>
    </tr>
}

</table>
