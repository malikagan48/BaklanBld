@model IEnumerable<BaklanWeb.Models.Model.Muhtarlik>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MuhtarlikAciklama)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MuhtarlikAciklama)
        </td>
        <td>
            @Html.ActionLink("Güncelle", "Edit", new { id=item.MuhtarlikId })
        </td>
    </tr>
}

</table>
