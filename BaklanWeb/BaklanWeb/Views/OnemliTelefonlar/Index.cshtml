@model IEnumerable<BaklanWeb.Models.Model.OnemliTelefonlar>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OnemliTelefonlarAciklama)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OnemliTelefonlarAciklama)
        </td>
        <td>
            @Html.ActionLink("Güncelle", "Edit", new { id=item.OnemliTelefonlarId })
        </td>
    </tr>
}

</table>
