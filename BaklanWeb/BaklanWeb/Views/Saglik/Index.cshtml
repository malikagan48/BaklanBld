@model IEnumerable<BaklanWeb.Models.Model.Saglik>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SaglikAciklama)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SaglikAciklama)
        </td>
        <td>
            @Html.ActionLink("Güncelle", "Edit", new { id=item.SaglikId }) 
        </td>
    </tr>
}

</table>
